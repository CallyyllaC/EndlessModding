<UserControl x:Class="EndlessModding.EndlessSpace2.Workshop.WorkshopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:c="Windows.UI.Xaml.Controls"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:en="EndlessModding.EndlessSpace2.Common.Classes.Amplitude_Runtime"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="1000">

    <!---<UserControl.Resources>
        <ResourceDictionary>
            <ObjectDataProvider x:Key="dataFromEnum" MethodName="GetValues"
                                    ObjectType="{x:Type sys:Enum}">
                <ObjectDataProvider.MethodParameters>
                    <x:Type TypeName="en:RuntimeModuleType"/>
                </ObjectDataProvider.MethodParameters>
            </ObjectDataProvider>
        </ResourceDictionary>
    </UserControl.Resources>-->

    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <Grid MinHeight="200" MinWidth="400" MaxHeight="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}, Path=ActualHeight}" MaxWidth="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}, Path=ActualWidth}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <GroupBox Header="Modules">
                    <ListView ItemsSource="{Binding Mods}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Path=Name}" />
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </GroupBox>
            </Grid>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <GroupBox Header="Workshop Directory" Grid.Row="0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Workshop Directory: " Grid.Column="0" Grid.Row="0"/>

                        <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding LocOutDir_Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                        <Button Grid.Column="2" Grid.Row="0" Content="Browse" Command="{Binding ButtonOutDirClick}"/>

                    </Grid>
                </GroupBox>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <GroupBox Header="Mod Info" Grid.Column="0">
                        <StackPanel Grid.Column="0">
                            <TextBlock TextWrapping="Wrap" Text="Type"/>
                            <ComboBox /><!--ItemsSource="{Binding Source{StaticResource dataFromEnum}}" SelectedItem="{Binding Type}"-->
                            <TextBlock TextWrapping="Wrap" Text="Title"/>
                            <TextBox TextWrapping="Wrap" Text="{Binding Title}"/>
                            <TextBlock TextWrapping="Wrap" Text="Description"/>
                            <TextBox TextWrapping="Wrap" Text="{Binding Description}"/>
                            <TextBlock TextWrapping="Wrap" Text="Author"/>
                            <TextBox TextWrapping="Wrap" Text="{Binding Author}"/>
                            <TextBlock TextWrapping="Wrap" Text="Release Notes"/>
                            <TextBox TextWrapping="Wrap" Text="{Binding ReleaseNotes}"/>
                            <TextBlock TextWrapping="Wrap" Text="Version"/>
                            <TextBox TextWrapping="Wrap" Text="{Binding Version}"/>
                        </StackPanel>
                    </GroupBox>

                    <Grid Grid.Column="1">

                        <GroupBox Header="Image Info">
                            <StackPanel>
                                <Canvas MinWidth="200" MinHeight="200" MaxWidth="430" MaxHeight="430">
                                    <Image Source="{Binding Image}"/>
                                </Canvas>

                                <Button Content="Browse Image" Command="{Binding ButtonOutDirClick}"/>
                            </StackPanel>
                        </GroupBox>
                    </Grid>

                    <Grid Grid.Column="2">
                        <GroupBox Header="Modules To Export">
                            <ListView ItemsSource="{Binding Modules}" ScrollViewer.CanContentScroll="True">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Path=Name}" />
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </GroupBox>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
